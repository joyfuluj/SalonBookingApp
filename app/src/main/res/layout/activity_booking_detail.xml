<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Booking Detail Title -->
    <TextView
        android:id="@+id/booking_detail_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Booking Details"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingBottom="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Salon Name -->
    <TextView
        android:id="@+id/detail_salon_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Salon: Salon A"
        android:textSize="18sp"
        android:layout_marginBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/booking_detail_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Menu -->
    <TextView
        android:id="@+id/detail_menu"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Menu: Coupon A"
        android:textSize="18sp"
        android:layout_marginBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/detail_salon_name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Stylist -->
    <TextView
        android:id="@+id/detail_stylist"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Stylist: Stylist A"
        android:textSize="18sp"
        android:layout_marginBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/detail_menu"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Date & Time -->
    <TextView
        android:id="@+id/detail_date_time"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Date &amp; Time: 2024/11/03(Sun) 11:00am - 12:00pm"
        android:textSize="18sp"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/detail_stylist"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Salon Info Button -->
    <Button
        android:id="@+id/button_view_salon_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="View Salon Info"
        android:background="@color/light_green"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/detail_date_time"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
